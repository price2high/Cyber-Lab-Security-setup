# Hydra Bruteforce Example

Hydra is used to perform brute-force attacks on login services.

- `hydra -l admin -P passwords.txt ssh://target_ip`
- Common protocols: ssh, ftp, http-get, http-post, rdp
